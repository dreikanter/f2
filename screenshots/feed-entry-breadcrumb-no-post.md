# feed-entry-breadcrumb-no-post.png

URL: `/feed_entries/:id` (desktop, 1280px)
State: pending feed entry with no associated post yet
Shows: no breadcrumb and no back-to-post navigation; just the status badge and the raw entry data
