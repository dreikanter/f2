# feed-entry-breadcrumb-before.png

URL: `/feed_entries/:id` (desktop, 1280px)
State: feed entry with an associated post
Shows: navigation back to the post before the breadcrumb change (a "Back to Post" button, no breadcrumb)
