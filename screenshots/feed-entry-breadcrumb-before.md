# feed-entry-breadcrumb-before.png

URL: `/feed_entries/:id` (desktop, 1280px)
State: processed feed entry with an associated post, before the breadcrumb change
Shows: no breadcrumb above the title; navigation back to the post is a "Back to Post" button next to the heading
