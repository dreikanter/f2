# feeds-empty-no-token.png

URL: `/feeds` (desktop, 1280px, full page)
State: signed-in user with zero feeds and no active access token
Shows: empty-page note plus the extra "you'll also need a token" line and the in-flow "New Feed" action; the header button is hidden
