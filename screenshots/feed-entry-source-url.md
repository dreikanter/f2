# feed-entry-source-url.png

URL: `/feed_entries/:id` (desktop, 1280px)
State: pending feed entry whose raw data includes a normal-length source URL
Shows: the source URL rendered as a link under the status badge; "Back to Post" is disabled since there's no post yet
