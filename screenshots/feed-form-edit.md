# feed-form-edit.png

URL: `/feeds/:id/edit` (desktop, 1280px, full page)
State: signed-in user editing an enabled RSS feed ("Google Open Source Blog")
Shows: editable source URL with re-check note, read-only detected feed type, feed name, "Preview feed contents" action, reposting settings (access token, target group, check interval), advanced options (skip older posts, only posts with images), and the "Enable feed" checkbox checked
