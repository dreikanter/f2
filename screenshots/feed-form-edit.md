# feed-form-edit.png

URL: `/feeds/:id/edit` (desktop, 1280px, full page)
State: signed-in user editing an enabled RSS feed
Shows: editable source URL, read-only detected feed type, feed name, "Preview feed contents" action, reposting settings, advanced options, and the "Enable feed" checkbox checked
