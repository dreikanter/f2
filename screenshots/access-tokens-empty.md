# access-tokens-empty.png

URL: `/access_tokens` (desktop, 1280px, full page)
State: signed-in user with zero access tokens
Shows: purpose note and the in-flow "New Token" action; the header button is hidden
