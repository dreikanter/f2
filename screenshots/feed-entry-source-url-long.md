# feed-entry-source-url-long.png

URL: `/feed_entries/:id` (desktop, 1280px)
State: feed entry with an extremely long source URL (long subdomain, path, and query string)
Shows: the URL link wraps across multiple lines without overflowing or breaking the page layout
