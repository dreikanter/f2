# posts-empty-no-enabled-feeds.png

URL: `/posts` (desktop, 1280px, full page)
State: signed-in user with zero posts and at least one feed, none of them enabled (e.g. a single draft feed)
Shows: "Enable one of your feeds" note linking to /feeds; sort and filter controls are hidden
