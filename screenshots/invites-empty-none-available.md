# invites-empty-none-available.png

URL: `/invites` (desktop, 1280px, full page)
State: signed-in user with `available_invites: 0` and no invites created
Shows: only the "No invites available." summary and the disabled "Invite a user" button; no empty-list note
