# invites-empty-available.png

URL: `/invites` (desktop, 1280px, full page)
State: signed-in user with `available_invites: 5` and no invites created
Shows: quota summary, the empty-list note, and the enabled "Invite a user" button
