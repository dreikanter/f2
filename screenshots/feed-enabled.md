# feed-enabled.png

URL: `/feeds/:id` (desktop, 1280px, full page)
State: enabled feed with an active access token, a target group, and some history
Shows: the feed page in its working state, with the target group linking out to the group on the token's FreeFeed instance
