# feed-form-new.png

URL: `/feeds/new` (desktop, 1280px, full page)
State: signed-in user creating a webhook-only feed (no source URL); form is blank
Shows: source note explaining the webhook endpoint, feed name field, access token and target group selects with a group refresh action, and the "Enable feed" checkbox unchecked
