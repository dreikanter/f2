# feed-entry-source-url-long-mobile.png

URL: `/feed_entries/:id` (mobile, ~390px)
State: same pending feed entry with an extremely long source URL, on a mobile viewport
Shows: the URL wraps within the narrow screen without horizontal scrolling; the nav collapses to the hamburger menu
