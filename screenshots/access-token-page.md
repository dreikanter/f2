# access-token-page.png

URL: `/access_tokens/:id` (desktop, 1280px, full page)
State: signed-in user viewing an active token that validated successfully, with available groups and associated feeds present
Shows: "Token is valid" banner, token details table, Publisher card, Available Groups list with refresh action, and the Associated Feeds list
