# access-token-page.png

URL: `/access_tokens/:id` (desktop, 1280px, full page)
State: signed-in user viewing an active token that validated successfully, with four available groups and five associated feeds
Shows: "Token is valid" banner, token details table (FreeFeed user, instance, last used, created), Publisher card, Available Groups list with refresh action, and the Associated Feeds list with per-feed status lines
