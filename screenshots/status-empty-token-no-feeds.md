# status-empty-token-no-feeds.png

URL: `/status` (desktop, 1280px, full page)
State: signed-in user with an active access token and zero feeds
Shows: dashboard intro, "now add a feed" note, and the "Add a feed" action
