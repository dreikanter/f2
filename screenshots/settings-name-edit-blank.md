# settings-name-edit-blank.png

URL: `/settings/name_update/edit` (desktop, 1280px, full page)
State: signed-in user with no name set
Shows: the empty name field and the note on where the name turns up
