# search-credentials-empty.png

URL: `/search_credentials` (desktop, 1280px, full page)
State: signed-in user with zero search credentials
Shows: empty note and the in-flow "New Credential" action; the header button is hidden
