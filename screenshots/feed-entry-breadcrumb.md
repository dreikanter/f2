# feed-entry-breadcrumb.png

URL: `/feed_entries/:id` (desktop, 1280px)
State: processed feed entry with an associated post
Shows: post breadcrumb above the title linking back to the post, replacing the "Back to Post" button
