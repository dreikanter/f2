# status-empty-draft-feeds.png

URL: `/status` (desktop, 1280px, full page)
State: signed-in user with an active access token and one feed in the draft state (no enabled or disabled feeds)
Shows: draft note and the "Continue setup" action, which links to the draft's edit page (feeds list when several drafts exist)
