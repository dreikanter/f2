# feed-disabled-no-token.png

URL: `/feeds/:id` (desktop, 1280px, full page)
State: feed with a target group whose access token was deleted, which leaves the feed disabled and tokenless
Shows: the target group still named but no longer linked, above the warning naming what the feed needs before it can be enabled
