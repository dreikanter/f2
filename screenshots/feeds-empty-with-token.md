# feeds-empty-with-token.png

URL: `/feeds` (desktop, 1280px, full page)
State: signed-in user with zero feeds and an active access token
Shows: same as feeds-empty-no-token but without the token line
