<%# locals: (can_create:) %>
<%= button_to "Invite a user",
              invites_path,
              method: :post,
              class: class_names(primary_button_classes(compact: true), "cursor-pointer disabled:cursor-not-allowed disabled:opacity-50"),
              disabled: !can_create,
              form: { data: { turbo_frame: "_top" } } %>
