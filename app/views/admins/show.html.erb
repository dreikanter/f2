<% content_for :title, "Admin Panel" %>

<div class="space-y-8">
  <%= render PageHeaderComponent.new(title: "Admin Panel") %>

  <section class="space-y-4">
    <%= render GlobalStatsComponent.new %>
    <%= render PostsHeatmapComponent.new %>
  </section>

  <div class="grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3">
    <%= render LinkedCardComponent.new(href: admin_users_path) do %>
      <div class="space-y-4">
        <%= h2 class: "flex items-center gap-2" do %>
          <%= icon("users", css_class: "size-5") %>
          <span>Users</span>
        <% end %>
        <p class="text-body">Manage user accounts and permissions</p>
      </div>
    <% end %>

    <%= render LinkedCardComponent.new(href: admin_events_path) do %>
      <div class="space-y-4">
        <%= h2 class: "flex items-center gap-2" do %>
          <%= icon("calendar", css_class: "size-5") %>
          <span>Events Log</span>
        <% end %>
        <p class="text-body">View system events, logs, and activity</p>
      </div>
    <% end %>

    <%= render LinkedCardComponent.new(href: admin_feeds_path) do %>
      <div class="space-y-4">
        <%= h2 class: "flex items-center gap-2" do %>
          <%= icon("layers", css_class: "size-5") %>
          <span>Feeds</span>
        <% end %>
        <p class="text-body">Browse feeds across every account</p>
      </div>
    <% end %>
  </div>
</div>
