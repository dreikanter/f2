<%# locals: (access_token:) %>
<% if access_token.owner.present? %>
  <span class="text-sm text-slate-500"><%= access_token.owner %></span>
<% else %>
  <span class="text-sm text-slate-400">—</span>
<% end %>
