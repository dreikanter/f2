<%# locals: (feed:) %>
<div class="card mt-3" data-controller="form-collapse autofocus">
  <div class="card-header">
    <h5 class="card-title mb-0">Edit Source</h5>
  </div>
  <div class="card-body">
    <%= form_with model: feed, class: "needs-validation", novalidate: true do |form| %>
  <%= hidden_field_tag :section, "content-source" %>
  <% if feed.errors.any? %>
    <div class="alert alert-danger">
      <h6><%= pluralize(feed.errors.count, "error") %> occurred:</h6>
      <ul class="mb-0">
        <% feed.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :name, "Title", class: "form-label fw-bold" %>
    <%= form.text_field :name, class: "form-control", placeholder: "Feed title for identification", maxlength: Feed::NAME_MAX_LENGTH, autofocus: true %>
    <div class="form-text">Give your feed a descriptive name</div>
  </div>

  <div class="mb-3">
    <%= form.label :url, "Feed URL", class: "form-label fw-bold" %>
    <%= form.url_field :url, class: "form-control", required: true, placeholder: "https://example.com/feed.xml" %>
    <div class="invalid-feedback">Provide a valid HTTP or HTTPS URL.</div>
  </div>

  <div class="mb-3">
    <%= form.label :feed_profile_key, "Feed Profile", class: "form-label fw-bold" %>
    <%= form.select :feed_profile_key, options_for_select(feed_profile_options, feed.feed_profile_key), { prompt: "Choose feed profile..." }, { class: "form-select", required: true } %>
    <div class="form-text">Select the processing configuration for this feed</div>
    <div class="invalid-feedback">Select a feed profile.</div>
  </div>

  <div class="d-flex gap-2">
    <%= form.submit "Save", class: "btn btn-primary" %>
    <button type="button" class="btn btn-outline-secondary" data-action="click->form-collapse#cancel">Cancel</button>
  </div>
    <% end %>
  </div>
</div>
