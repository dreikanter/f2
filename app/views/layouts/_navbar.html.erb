<nav class="bg-slate-100 border-b border-slate-200">
  <div class="flex flex-wrap items-center justify-between mx-auto px-4 py-4">
    <%= link_to "F2", root_path, class: class_names("ff-nav-brand", "ff-focus-ring") %>

    <button data-collapse-toggle="primary-navigation" type="button" class="inline-flex items-center justify-center rounded-lg p-2 text-sm text-slate-600 hover:bg-slate-100 focus:outline-none focus:ring-2 focus:ring-slate-200 md:hidden" aria-controls="primary-navigation" aria-expanded="false">
      <span class="sr-only">Toggle navigation</span>
      <svg class="h-5 w-5" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 17 14">
        <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M1 1h15M1 7h15M1 13h15" />
      </svg>
    </button>

    <div class="hidden w-full md:block md:w-auto" id="primary-navigation">
      <ul class="mt-4 flex flex-col gap-1 rounded-lg border border-slate-200 bg-white p-4 text-lg font-medium text-slate-600 md:mt-0 md:flex-row md:items-center md:space-x-4 md:border-0 md:bg-transparent md:p-0">
        <% navbar_items.each do |item| %>
          <% link_classes = class_names(
            "ff-nav-link",
            { "ff-nav-link--active" => item[:active] },
            "ff-focus-ring"
          ) %>
          <li>
            <%= link_to item[:name], item[:path], class: link_classes, aria: (item[:active] ? { current: "page" } : nil) %>
          </li>
        <% end %>
        <% settings_link_classes = class_names(
          "ff-nav-link",
          { "ff-nav-link--active" => current_page?(settings_path) },
          "ff-focus-ring"
        ) %>
        <li class="md:ml-auto">
          <%= link_to "Settings", settings_path, class: settings_link_classes, aria: (current_page?(settings_path) ? { current: "page" } : nil) %>
        </li>
      </ul>
    </div>
  </div>
</nav>
