<!DOCTYPE html>
<html lang="en">
  <head>
    <%= render "layouts/head" %>
  </head>
  <body class="min-h-screen bg-white text-slate-900 sm:bg-slate-100">
    <%= render "layouts/flash" %>

    <main class="mx-auto w-full max-w-2xl px-6 py-3 sm:px-6 sm:py-6" data-controller="autofocus">
      <%= yield %>
    </main>
  </body>
</html>
