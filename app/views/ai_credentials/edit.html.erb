<% content_for :title, "Edit AI Credential" %>

<div class="space-y-8" data-key="ai_credentials.edit">
  <%= render PageHeaderComponent.new(title: "Edit credential") do |header| %>
    <% header.with_context_paragraph("Update the name or replace the API key. The key will be re-checked after saving.") %>
  <% end %>

  <div class="max-w-2xl">
    <%= form_with model: @credential do |f| %>
      <% if @credential.errors.any? %>
        <%= render AlertComponent.new(variant: :warning) do %>
          <ul class="list-disc list-inside text-sm">
            <% @credential.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        <% end %>
      <% end %>

      <div class="space-y-10">
        <div class="space-y-2">
          <%= label_tag "ai_credential_provider", "AI Provider", class: form_label_classes %>
          <%= text_field_tag "ai_credential_provider",
                             @credential.llm_provider.display_name,
                             disabled: true,
                             class: input_field_classes(disabled: true) %>
        </div>

        <div class="space-y-2">
          <%= f.label :display_name, "Name", class: form_label_classes %>
          <%= f.text_field :display_name,
                           placeholder: "Personal Anthropic key",
                           autofocus: true,
                           class: input_field_classes,
                           required: true,
                           data: { key: "ai_credentials.display-name" } %>
        </div>

        <div class="space-y-2">
          <%= label_tag "ai_credential_credential_data_api_key", "API key", class: form_label_classes %>
          <%= password_field_tag "ai_credential[credential_data][api_key]",
                                 nil,
                                 id: "ai_credential_credential_data_api_key",
                                 class: input_field_classes,
                                 autocomplete: "off",
                                 data: { key: "ai_credentials.credential-data.api_key" } %>
          <p class="text-sm text-muted">Leave blank to keep the current key.</p>
        </div>
      </div>

      <div class="mt-6 flex flex-wrap items-center justify-start gap-4">
        <%= f.submit "Update Credential",
                     class: primary_button_classes,
                     data: { turbo_submits_with: "Saving…" } %>
        <%= link_to "Cancel", ai_credential_path(@credential), class: secondary_button_classes %>
      </div>
    <% end %>
  </div>
</div>
