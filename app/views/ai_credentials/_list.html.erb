<%# locals: (credentials:) %>
<%# The empty-state note lives in the index page header; nothing to render
    without records. %>
<%= render AiCredentialsListComponent.new(credentials: credentials) if credentials.any? %>
