<% page_title = "Posts" %>
<% content_for :title, page_title %>

<div class="space-y-8">
  <%= render PageHeaderComponent.new(title: page_title) do |header| %>
    <% if @feed %>
      <% group = @feed.target_group.presence %>
      <% label = @feed.display_name + (group ? " (@#{group})" : "") %>
      <% header.with_context_paragraph("Posts from #{label}.") %>
    <% else %>
      <% header.with_context_paragraph("Everything Feeder imports from your feeds shows up here, along with each post's status on FreeFeed.") %>
    <% end %>
    <% if @posts.empty? %>
      <% if @feed %>
        <% header.with_context_paragraph("No posts imported yet from this feed.") %>
      <% elsif @feeds.any?(&:enabled?) %>
        <% header.with_context_paragraph("No posts yet — they'll show up here as your feeds import new content.") %>
      <% elsif @feeds.any? %>
        <% header.with_context_paragraph do %>
          No posts yet. <%= link_to "Enable one of your feeds", feeds_path, class: "font-medium text-brand underline underline-offset-4 transition hover:text-brand-hover" %> and new content will show up here.
        <% end %>
      <% else %>
        <% header.with_context_paragraph("No posts yet — add a feed to start importing content.") %>
      <% end %>
    <% end %>
    <% if @feeds.any? && (@posts.any? || @feed) %>
      <% header.with_action_button do %>
        <%= render FeedFilterDropdownComponent.new(
              feeds: @feeds,
              selected_feed: @feed,
              sort_params: params.permit(:sort, :direction),
              menu_id: "posts-feed-menu"
            ) %>
      <% end %>
    <% end %>
    <% if @posts.any? %>
      <% header.with_action_button do %>
        <%= render SortDropdownComponent.new(presenter: @sortable_presenter, menu_id: "posts-sort-menu") %>
      <% end %>
    <% end %>
  <% end %>

  <% if @posts.any? %>
    <%= render PostsListComponent.new(posts: @posts, show_feed: @feed.blank?) %>
  <% elsif @feeds.none? %>
    <div>
      <%= link_to "Add a feed", new_feed_path, class: "inline-flex items-center justify-center whitespace-nowrap rounded-md bg-brand px-6 py-3 text-base font-semibold text-on-brand shadow-sm transition hover:bg-brand-hover focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-1", data: { key: "posts.empty.new-feed" } %>
    </div>
  <% end %>

  <% if pagination_total_pages > 1 %>
    <div class="text-center text-sm text-body">
      <%= @posts.size %> of <%= pagination_total_count %> posts
    </div>
  <% end %>

  <%= render_pagination { |pagination, params| posts_path(pagination.merge(feed_id: params[:feed_id])) } %>
</div>
