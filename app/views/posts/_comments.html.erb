<% comments.each do |comment| %>
  <div class="flex items-start gap-2" data-key="<%= key %>">
    <%= icon("message-circle", css_class: "mt-1 size-3.5 text-muted") %>
    <div class="min-w-0 flex-1 text-heading"><%= format_post_content(comment) %></div>
  </div>
<% end %>
