<% if @delete_record %>
  <%= turbo_stream.remove dom_id(@post) %>
<% else %>
  <%= turbo_stream.replace dom_id(@post) do %>
    <%= render PostListItemComponent.new(post: @post, show_feed: true) %>
  <% end %>
<% end %>

<%= turbo_stream.replace "flash-messages", partial: "layouts/flash" %>
