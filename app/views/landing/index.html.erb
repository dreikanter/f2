<% content_for :title, "Welcome" %>

<div class="flex justify-center my-8">
  <div class="w-full max-w-2xl space-y-8 text-center">
    <div class="space-y-4">
      <h1 class="text-4xl font-bold text-heading sm:text-5xl">Welcome to Feeder</h1>
      <p class="text-xl text-body">A reposting service for FreeFeed.</p>
    </div>

    <div class="flex justify-center">
      <%= link_to "Sign In", new_session_path, class: "inline-flex items-center justify-center whitespace-nowrap rounded-md bg-brand px-8 py-4 text-lg font-semibold text-on-brand shadow-sm transition hover:bg-brand-hover focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-1" %>
    </div>
  </div>
</div>
