<% content_for :title, "Welcome to Feeder" %>

<h1 class="card-title mb-4">Welcome to Feeder</h1>

<p class="lead mb-4">
  Feeder automatically reposts content from RSS, Atom, and other sources to your FreeFeed groups.
</p>

<p class="mb-4">
  Getting started is straightforward. First, connect your FreeFeed account with an API token. Then add a feed to monitor. That's it.
</p>

<p class="mb-4">
  Ready to add your first token?
</p>

<div class="d-flex gap-3">
  <%= link_to "Add token", new_settings_access_token_path, class: "btn btn-primary" %>
  <%= button_to "Skip setup", onboarding_path, method: :delete, form: { data: { turbo_confirm: "Skip the guided setup?" } }, class: "btn btn-light text-muted" %>
</div>
