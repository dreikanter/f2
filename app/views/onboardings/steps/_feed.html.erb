<% content_for :title, "Add a Feed" %>

<h1 class="card-title mb-4">Token added</h1>

<p class="lead mb-4">
  Nice. Your FreeFeed account is connected.
</p>

<p class="mb-4">
  Now let's add your first feed. Pick any RSS or Atom feed you want to monitor, and Feeder will automatically post new content to your FreeFeed groups.
</p>

<div class="d-flex gap-3">
  <%= link_to "Add feed", new_feed_path, class: "btn btn-primary" %>
  <%= button_to "Skip setup", onboarding_path, method: :delete, form: { data: { turbo_confirm: "Skip the guided setup?" } }, class: "btn btn-light text-muted" %>
</div>
