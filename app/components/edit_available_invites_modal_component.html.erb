<%= render ModalComponent.new(title: "Update Available Invites", modal_id: @modal_id) do |modal| %>
  <%= form_with(model: @user, url: admin_user_available_invites_path(@user), method: :patch, id: "available-invites-form-#{@user.id}", data: { turbo_frame: "_top" }) do |form| %>
    <div class="space-y-2">
      <%= form.label :available_invites, "Available Invites", class: helpers.form_label_classes %>
      <span id="available-invites-input-wrapper-<%= @user.id %>">
        <%= form.number_field(:available_invites, min: 0, class: helpers.input_field_classes, autofocus: true) %>
      </span>
    </div>
  <% end %>

  <% modal.with_footer do %>
    <%= tag.button "Update",
                   type: "submit",
                   form: "available-invites-form-#{@user.id}",
                   class: helpers.primary_button_classes(compact: true),
                   data: { action: "modal#confirm" } %>
    <%= tag.button "Cancel",
                   type: "button",
                   class: helpers.secondary_button_classes(compact: true),
                   data: { action: "modal#close" } %>
  <% end %>
<% end %>
