<%= tag.details(**container_options) do %>
  <summary class="flex cursor-pointer select-none items-center justify-between gap-3 py-5 font-medium text-body transition group-open:text-heading marker:content-none [&::-webkit-details-marker]:hidden">
    <span><%= @title %></span>
    <%= helpers.icon("chevron-down", css_class: "size-5 text-faint transition group-open:rotate-180") %>
  </summary>
  <div class="border-t border-border py-5">
    <%= content %>
  </div>
<% end %>
